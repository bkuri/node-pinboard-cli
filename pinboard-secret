#!/usr/bin/coffee
'use strict'


{argv, define, pinboard, render, verify} = (require './tools')

verify()

(define 'Returns the user\'s secret RSS key (for viewing private feeds).')
  .action ->
    try
      pinboard().userSecret(render)

    catch e
      render e, 'error'

  .parse argv()
