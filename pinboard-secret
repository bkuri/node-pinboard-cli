#!/usr/bin/coffee
'use strict'


{argv, define, pinboard, respond, verify} = (require './tools')

verify()

(define 'Returns the user\'s secret RSS key (for viewing private feeds).')
  .action ->
    try
      pinboard().userSecret(respond)

    catch e
      respond e, 'error'

  .parse argv()
